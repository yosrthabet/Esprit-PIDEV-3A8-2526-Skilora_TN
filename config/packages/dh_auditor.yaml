# Full configuration reference available at:
# https://damienharper.github.io/auditor-docs/docs/auditor-bundle/configuration/reference.html
dh_auditor:
    enabled: true
    providers:
        doctrine:
            table_prefix: null
            table_suffix: '_audit'
            # Sans ceci, aucune route n’est enregistrée (dh_auditor_list_audits, etc.)
            viewer:
                enabled: true
